kernelOption:
    - forceRun

timeOut: 1000

compileOption:
    instSelMethod:
      - customInstselector:
          include:
            - InvalidSender(MPI)

    regSelMethod: customregselector
    customRegSelector: Automatic

runOption:
    - run:
        numOfRuns: 5
        fi_type: AutoInjection
