defaultTimeOut: 1000

compileOption:
    instSelMethod:
      - customInstselector:
          include:
            - InvalidPointer(Res)

    regSelMethod: customregselector
    customRegSelector: Automatic

runOption:
    - run:
        numOfRuns: 1
        fi_type: AutoInjection
