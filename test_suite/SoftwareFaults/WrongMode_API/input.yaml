defaultTimeOut: 1000

compileOption:
    instSelMethod:
      - customInstselector:
          include:
            - WrongMode(API)

    regSelMethod: customregselector
    customRegSelector: Automatic

runOption:
    - run:
        numOfRuns: 3
        fi_type: AutoInjection
