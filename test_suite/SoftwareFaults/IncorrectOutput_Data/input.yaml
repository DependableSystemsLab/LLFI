defaultTimeOut: 1000

compileOption:
    instSelMethod:
      - customInstselector:
          include:
            - IncorrectOutput(Data)

    regSelMethod: customregselector
    customRegSelector: Automatic

runOption:
    - run:
        numOfRuns: 2
        fi_type: AutoInjection
