defaultTimeOut: 1000

compileOption:
    instSelMethod:
      - customInstselector:
          include:
            - WrongSavedFormat(IO)

    regSelMethod: customregselector
    customRegSelector: Automatic

runOption:
    - run:
        numOfRuns: 3
        fi_type: AutoInjection
