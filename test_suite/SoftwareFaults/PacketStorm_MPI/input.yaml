defaultTimeOut: 1000

compileOption:
    instSelMethod:
      - customInstselector:
          include:
            - PacketStorm(MPI)

    regSelMethod: customregselector
    customRegSelector: Automatic

runOption:
    - run:
        numOfRuns: 3
        fi_type: AutoInjection
