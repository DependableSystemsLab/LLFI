defaultTimeOut: 50

compileOption:
    instSelMethod:
      - customInstselector:
          include:
            - PacketStorm(MPI)

    regSelMethod: customregselector
    customRegSelector: Automatic

runOption:
    - run:
        numOfRuns: 2
        fi_type: AutoInjection
